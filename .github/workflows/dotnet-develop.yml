# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET Develop

env:
  BUILD_CONFIGURATION: Debug

on:
  push:
    branches: [ "develop" ]
  pull_request:
    branches: [ "develop" ]
  workflow_dispatch:
    
permissions:
  contents: read
  pages: write
  id-token: write
  checks: write
  pull-requests: write

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x
    - name: Restore dependencies
      run: dotnet restore src/adr-tool.slnx
    - name: Build
      run: dotnet build src/adr-tool.slnx --no-restore --configuration ${{ env.BUILD_CONFIGURATION }}
    - name: Test
      run: dotnet test src/adr-tool.slnx --no-build --verbosity normal --collect:"XPlat Code Coverage" --logger:"junit;LogFilePath=junit.xml" /p:CollectCoverage=true /p:CoverletOutputFormat=cobertura --results-directory ./TestResults --configuration ${{ env.BUILD_CONFIGURATION }}
    - name: Pack
      run: dotnet pack src/adr-tool.slnx --configuration ${{ env.BUILD_CONFIGURATION }}
    - name: Run Codacy Analysis CLI
      uses: codacy/codacy-analysis-cli-action@master
    - name: Publish Test Results
      uses: EnricoMi/publish-unit-test-result-action@v2
      if: always()
      with:
        files: "**/junit.xml"
    - name: ReportGenerator
      uses: danielpalme/ReportGenerator-GitHub-Action@5
      with:
        reports: ./TestResults/**/coverage.cobertura.xml
        targetdir: ./TestResults/CoverageReport
        reporttypes: Html;MarkdownSummaryGithub
    - name: Write Coverage to Job Summary
      run: cat ./TestResults/CoverageReport/SummaryGithub.md >> $GITHUB_STEP_SUMMARY
    - name: Upload Coverage Report
      uses: actions/upload-artifact@v4
      with:
        name: coverage-report
        path: ./TestResults/CoverageReport